/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./js/main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./js/components/burger.js":
/*!*********************************!*\
  !*** ./js/components/burger.js ***!
  \*********************************/
/*! exports provided: Burger */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Burger", function() { return Burger; });
class Burger {
  constructor() {
    this.init();
  }
  init() {
    const toggle = document.getElementById('toggle');
    const header = document.querySelector('header');
    const mainNav = document.querySelector('.main-nav');
    const menuList = document.querySelector('.main-nav__list');
    if (!toggle || !header || !mainNav || !menuList) {
      return;
    }

    // Убеждаемся, что лоадер завершился
    if (document.body.classList.contains('loading')) {
      // Не добавляем loaded здесь, пусть лоадер сам управляет этим
    }
    const openMenu = () => {
      // Убираем класс loading если он есть
      if (document.body.classList.contains('loading')) {
        // Не добавляем loaded здесь, пусть лоадер сам управляет этим
      }
      header.classList.add('header--openned');
      menuList.classList.add('is-open');
      toggle.setAttribute('aria-pressed', 'true');
    };
    const closeMenu = () => {
      header.classList.remove('header--openned');
      menuList.classList.remove('is-open');
      toggle.setAttribute('aria-pressed', 'false');
    };
    const toggleMenu = () => {
      if (header.classList.contains('header--openned')) {
        closeMenu();
      } else {
        openMenu();
      }
    };

    // Toggle by button
    toggle.addEventListener('click', event => {
      event.stopPropagation();
      toggleMenu();
    });

    // Click outside closes menu
    document.addEventListener('click', event => {
      if (!header.classList.contains('header--openned')) {
        return;
      }
      const target = event.target;
      if (target instanceof Element) {
        if (!mainNav.contains(target)) {
          closeMenu();
        }
      }
    });

    // Esc closes menu
    document.addEventListener('keydown', event => {
      if (event.key === 'Escape' && header.classList.contains('header--openned')) {
        closeMenu();
      }
    });

    // Click on navigation link closes menu (mobile UX)
    mainNav.addEventListener('click', event => {
      const target = event.target;
      if (target instanceof Element && target.closest('.main-nav__link')) {
        closeMenu();
      }
    });
  }
}

/***/ }),

/***/ "./js/components/loader.js":
/*!*********************************!*\
  !*** ./js/components/loader.js ***!
  \*********************************/
/*! exports provided: Loader */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Loader", function() { return Loader; });
class Loader {
  constructor() {
    this.loader = document.getElementById('loader');
    this.minLoadTime = 1500; // Минимальное время показа лоадера (1.5 сек)
    this.startTime = Date.now();
    if (this.loader) {
      this.init();
    }
  }
  init() {
    // Добавляем класс загрузки для блокировки анимаций
    document.body.classList.add('loading');

    // Блокируем прокрутку при показе лоадера
    document.body.style.overflow = 'hidden';

    // Скрываем лоадер после полной загрузки страницы
    window.addEventListener('load', () => {
      this.hideLoader();
    });

    // Дополнительная проверка через 3 секунды на случай проблем с загрузкой
    setTimeout(() => {
      if (this.loader && !this.loader.classList.contains('hidden')) {
        this.hideLoader();
      }
    }, 3000);
  }
  hideLoader() {
    if (!this.loader) {
      return;
    }
    const elapsedTime = Date.now() - this.startTime;
    const remainingTime = Math.max(0, this.minLoadTime - elapsedTime);
    setTimeout(() => {
      this.loader.classList.add('hidden');

      // Восстанавливаем прокрутку
      document.body.style.overflow = '';

      // Запускаем анимации через 300ms после скрытия лоадера
      setTimeout(() => {
        // Активируем анимации после полного исчезновения лоадера
        document.body.classList.remove('loading');
        document.body.classList.add('loaded');
      }, 300);

      // Удаляем лоадер из DOM после анимации
      setTimeout(() => {
        if (this.loader && this.loader.parentNode) {
          this.loader.parentNode.removeChild(this.loader);
          // Убеждаемся, что лоадер не блокирует взаимодействие
          document.body.style.pointerEvents = '';
        }
      }, 500); // Время анимации из CSS
    }, remainingTime);
  }

  // Метод для принудительного скрытия лоадера (если нужно)
  forceHide() {
    if (this.loader) {
      this.loader.classList.add('hidden');

      // Активируем анимации на сайте
      document.body.classList.remove('loading');
      document.body.classList.add('loaded');

      // Восстанавливаем прокрутку
      document.body.style.overflow = '';
      setTimeout(() => {
        if (this.loader && this.loader.parentNode) {
          this.loader.parentNode.removeChild(this.loader);
        }
      }, 500);
    }
  }
}

/***/ }),

/***/ "./js/components/map.js":
/*!******************************!*\
  !*** ./js/components/map.js ***!
  \******************************/
/*! exports provided: Map */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Map", function() { return Map; });
class Map {
  constructor() {
    this.mapContainer = document.querySelector('.contacts__map');
    this.mapLoaded = false;
    if (this.mapContainer) {
      this.init();
    }
  }
  init() {
    // Intersection Observer для ленивой загрузки
    if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
          if (entry.isIntersecting && !this.mapLoaded) {
            // Карта видна, можно загружать
            this.loadMap();
            observer.unobserve(entry.target);
          }
        });
      }, {
        rootMargin: '50px'
      });
      observer.observe(this.mapContainer);
    } else {
      // Fallback для старых браузеров
      this.loadMap();
    }
  }
  loadMap() {
    if (this.mapLoaded) {
      return;
    }

    // Создаем скрипт для загрузки карты
    const script = document.createElement('script');
    script.type = 'text/javascript';
    script.charset = 'utf-8';
    script.async = true;
    script.src = 'https://api-maps.yandex.ru/services/constructor/1.0/js/?um=constructor%3A20e8497a73bf6c58f16d0ef9ed3d08c7c3d29cd14b63c690f568914451c029d3&width=100%&height=400&lang=ru_RU&scroll=true';

    // Обработчик успешной загрузки
    script.onload = () => {
      this.mapLoaded = true;
      this.mapContainer.classList.add('loaded');

      // Используем MutationObserver для отслеживания появления iframe
      const observer = new MutationObserver(mutations => {
        mutations.forEach(mutation => {
          if (mutation.type === 'childList') {
            const iframe = this.mapContainer.querySelector('iframe');
            if (iframe) {
              iframe.classList.add('loaded');
              observer.disconnect();
            }
          }
        });
      });
      observer.observe(this.mapContainer, {
        childList: true,
        subtree: true
      });

      // Fallback - если iframe не появится через 3 секунды
      setTimeout(() => {
        const iframe = this.mapContainer.querySelector('iframe');
        if (iframe) {
          iframe.classList.add('loaded');
        }
        observer.disconnect();
      }, 3000);
    };

    // Обработчик ошибки загрузки
    script.onerror = () => {
      this.mapContainer.classList.add('loaded');
    };

    // Добавляем скрипт в контейнер
    this.mapContainer.appendChild(script);

    // Fallback - скрываем спиннер через 5 секунд
    setTimeout(() => {
      if (!this.mapLoaded) {
        this.mapContainer.classList.add('loaded');
      }
    }, 5000);
  }
}

/***/ }),

/***/ "./js/components/promo-form.js":
/*!*************************************!*\
  !*** ./js/components/promo-form.js ***!
  \*************************************/
/*! exports provided: PromoForm */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "PromoForm", function() { return PromoForm; });
/* harmony import */ var _utils_telegram__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/telegram */ "./js/utils/telegram.js");

class PromoForm {
  constructor(username = 'feslevee26') {
    this.username = username;
    this.form = document.querySelector('.promo__form');
    if (this.form) {
      this.inputs = Array.from(this.form.querySelectorAll('.custom-input__field'));
      this.submitButton = this.form.querySelector('.promo__btn');
      this.handleSubmit = this.handleSubmit.bind(this);
      this.bindEvents();
    }
  }
  bindEvents() {
    this.form.addEventListener('submit', this.handleSubmit);
    if (this.submitButton) {
      this.submitButton.addEventListener('click', _e => {
        // Fallback if something prevents submit
        if (this.form) {
          // Let the submit handler run
        }
      });
    }
    this.inputs.forEach(input => {
      input.addEventListener('input', () => this.clearError(input));
    });
  }
  handleSubmit(event) {
    event.preventDefault();
    const {
      isValid,
      values,
      firstInvalid
    } = this.validate();
    if (!isValid) {
      if (firstInvalid) {
        firstInvalid.focus();
      }
      return;
    }
    const messageLines = ['Здравствуйте! Хочу пробную неделю!', values.name ? `Имя: ${values.name}` : '', values.phone ? `Телефон: ${values.phone}` : ''].filter(Boolean);
    const text = messageLines.join('\n');
    Object(_utils_telegram__WEBPACK_IMPORTED_MODULE_0__["openTelegram"])(this.username, text);
  }
  validate() {
    let isValid = true;
    const nameInput = /** @type {HTMLInputElement|null} */this.form.querySelector('input[type="text"]');
    const phoneInput = /** @type {HTMLInputElement|null} */this.form.querySelector('input[type="tel"]');
    const values = {
      name: nameInput ? nameInput.value.trim() : '',
      phone: phoneInput ? phoneInput.value.trim() : ''
    };
    let firstInvalid = null;

    // Name: 2+ chars
    if (!values.name || values.name.length < 2) {
      isValid = false;
      this.setError(nameInput, 'Введите имя (минимум 2 символа)');
      firstInvalid = firstInvalid || nameInput;
    }

    // Phone: digits length >= 10
    const digits = values.phone.replace(/\D/g, '');
    if (digits.length < 10) {
      isValid = false;
      this.setError(phoneInput, 'Укажите корректный телефон (не менее 10 цифр)');
      firstInvalid = firstInvalid || phoneInput;
    }
    return {
      isValid,
      values,
      firstInvalid
    };
  }
  setError(input, message) {
    if (!input) {
      return;
    }
    const wrapper = input.closest('.custom-input');
    if (wrapper) {
      wrapper.classList.add('is-invalid');
      let errorEl = wrapper.querySelector('.custom-input__error');
      const errorId = `${input.name || input.type}-error`;
      if (!errorEl) {
        errorEl = document.createElement('div');
        errorEl.className = 'custom-input__error';
        errorEl.id = errorId;
        wrapper.appendChild(errorEl);
      }
      errorEl.textContent = message;
      // ensure id stays in sync if element existed
      if (!errorEl.id) {
        errorEl.id = errorId;
      }
    }
    input.setAttribute('aria-invalid', 'true');
    input.setAttribute('aria-describedby', `${input.name || input.type}-error`);
  }
  clearError(input) {
    const wrapper = input.closest('.custom-input');
    if (wrapper) {
      wrapper.classList.remove('is-invalid');
      const errorEl = wrapper.querySelector('.custom-input__error');
      if (errorEl) {
        errorEl.remove();
      }
    }
    input.removeAttribute('aria-invalid');
    input.removeAttribute('title');
    input.removeAttribute('aria-describedby');
  }
}

/***/ }),

/***/ "./js/components/slider.js":
/*!*********************************!*\
  !*** ./js/components/slider.js ***!
  \*********************************/
/*! exports provided: Slider */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Slider", function() { return Slider; });
class Slider {
  constructor() {
    this.init();
  }
  init() {
    // Единый инстанс Swiper с breakpoint'ами
    const SwiperCtor = typeof window !== 'undefined' && window.Swiper ? window.Swiper : null;
    if (!SwiperCtor) {
      return;
    }
    new SwiperCtor('.swiper', {
      centeredSlides: false,
      speed: 600,
      effect: 'slide',
      grabCursor: true,
      navigation: {
        nextEl: '.swiper__button_next',
        prevEl: '.swiper__button_prev',
        disabledClass: 'swiper-button-disabled',
        hiddenClass: 'swiper-button-hidden'
      },
      scrollbar: {
        el: '.swiper-scrollbar',
        draggable: true,
        dragSize: 100,
        hide: false,
        snapOnRelease: true,
        dragClass: 'swiper-scrollbar-drag',
        progressbarFillClass: 'swiper-scrollbar-fill'
      },
      breakpoints: {
        0: {
          slidesPerView: 'auto',
          spaceBetween: 12,
          centeredSlides: true,
          centeredSlidesBounds: true,
          loop: false
        },
        768: {
          slidesPerView: 'auto',
          spaceBetween: 12,
          centeredSlides: false
        },
        1200: {
          slidesPerView: 'auto',
          spaceBetween: 24
        }
      }
    });
  }
}

/***/ }),

/***/ "./js/components/tickets.js":
/*!**********************************!*\
  !*** ./js/components/tickets.js ***!
  \**********************************/
/*! exports provided: Tickets */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Tickets", function() { return Tickets; });
class Tickets {
  constructor() {
    this.init();
  }
  init() {
    const list = document.querySelector('.tickets__list');
    if (!list) {
      return;
    }
    const setCardsTabbable = () => {
      list.querySelectorAll('.ticket-card').forEach(card => {
        if (!card.hasAttribute('tabindex')) {
          card.setAttribute('tabindex', '0');
        }
        card.setAttribute('role', 'button');
        card.setAttribute('aria-pressed', card.classList.contains('ticket-card--active') ? 'true' : 'false');
      });
    };
    const activateCard = targetCard => {
      const cards = list.querySelectorAll('.ticket-card');
      cards.forEach(c => {
        c.classList.remove('ticket-card--active');
        c.setAttribute('aria-pressed', 'false');
      });
      targetCard.classList.add('ticket-card--active');
      targetCard.setAttribute('aria-pressed', 'true');
    };
    list.addEventListener('click', event => {
      const btn = event.target.closest('.ticket-card__btn');
      if (btn) {
        return; // не переключаем по клику на кнопку внутри карточки
      }
      const card = event.target.closest('.ticket-card');
      if (!card || !list.contains(card)) {
        return;
      }
      activateCard(card);
    });
    list.addEventListener('keydown', event => {
      if (event.key !== 'Enter' && event.key !== ' ') {
        return;
      }
      const card = event.target.closest('.ticket-card');
      if (!card || !list.contains(card)) {
        return;
      }
      event.preventDefault();
      activateCard(card);
    });
    setCardsTabbable();
  }
}

/***/ }),

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _utils_ios_vh_fix__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils/ios-vh-fix */ "./js/utils/ios-vh-fix.js");
/* harmony import */ var _modules_modals_init_modals__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./modules/modals/init-modals */ "./js/modules/modals/init-modals.js");
/* harmony import */ var _components_burger__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./components/burger */ "./js/components/burger.js");
/* harmony import */ var _components_slider__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./components/slider */ "./js/components/slider.js");
/* harmony import */ var _components_tickets__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./components/tickets */ "./js/components/tickets.js");
/* harmony import */ var _utils_smoothScroll__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./utils/smoothScroll */ "./js/utils/smoothScroll.js");
/* harmony import */ var _utils_active_nav__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./utils/active-nav */ "./js/utils/active-nav.js");
/* harmony import */ var _components_promo_form__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./components/promo-form */ "./js/components/promo-form.js");
/* harmony import */ var _utils_telegram__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./utils/telegram */ "./js/utils/telegram.js");
/* harmony import */ var _components_map__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./components/map */ "./js/components/map.js");
/* harmony import */ var _components_loader__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./components/loader */ "./js/components/loader.js");












// ---------------------------------

window.addEventListener('DOMContentLoaded', () => {
  // Utils
  Object(_utils_ios_vh_fix__WEBPACK_IMPORTED_MODULE_0__["iosVhFix"])();
  Object(_utils_smoothScroll__WEBPACK_IMPORTED_MODULE_5__["smoothScroll"])();
  Object(_utils_active_nav__WEBPACK_IMPORTED_MODULE_6__["activeNav"])();
  Object(_utils_telegram__WEBPACK_IMPORTED_MODULE_8__["bindTelegramLinks"])('feslevee26');

  // Components
  new _components_loader__WEBPACK_IMPORTED_MODULE_10__["Loader"](); // инициализируем лоадер первым
  new _components_slider__WEBPACK_IMPORTED_MODULE_3__["Slider"]();
  new _components_tickets__WEBPACK_IMPORTED_MODULE_4__["Tickets"]();
  new _components_promo_form__WEBPACK_IMPORTED_MODULE_7__["PromoForm"]('feslevee26');
  new _components_map__WEBPACK_IMPORTED_MODULE_9__["Map"]();

  // Modules
  window.addEventListener('load', () => {
    Object(_modules_modals_init_modals__WEBPACK_IMPORTED_MODULE_1__["initModals"])();
    // Инициализируем бургер после полной загрузки
    new _components_burger__WEBPACK_IMPORTED_MODULE_2__["Burger"]();
  });
});

// ---------------------------------

// ❗❗❗ обязательно установите плагины eslint, stylelint, editorconfig в редактор кода.

// привязывайте js не на классы, а на дата атрибуты (data-validate)

// вместо модификаторов .block--active используем утилитарные классы
// .is-active || .is-open || .is-invalid и прочие (обязателен нейминг в два слова)
// .select.select--opened ❌ ---> [data-select].is-open ✅

// выносим все в дата атрибуты
// url до иконок пинов карты, настройки автопрокрутки слайдера, url к json и т.д.

// для адаптивного JS используейтся matchMedia и addListener
// const breakpoint = window.matchMedia(`(min-width:1024px)`);
// const breakpointChecker = () => {
//   if (breakpoint.matches) {
//   } else {
//   }
// };
// breakpoint.addListener(breakpointChecker);
// breakpointChecker();

// используйте .closest(el)

/***/ }),

/***/ "./js/modules/modals/init-modals.js":
/*!******************************************!*\
  !*** ./js/modules/modals/init-modals.js ***!
  \******************************************/
/*! exports provided: modals, initModals */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "modals", function() { return modals; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "initModals", function() { return initModals; });
/* harmony import */ var _modals__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./modals */ "./js/modules/modals/modals.js");

let modals;

// Здесь реализован пример открытия модалки через колбэк закрытия
// const openModalInCloseCallback = (name, context = this) => {
//   context._enableScrolling = false;
//   context._setSettings('default');
//   modals.open(name);
// };

// closeCallback() {
//   openModalInCloseCallback('modal-5');
// },

const settings = {
  'default': {
    preventDefault: true,
    stopPlay: true,
    lockFocus: true,
    startFocus: true,
    focusBack: true,
    eventTimeout: 400,
    openCallback: false,
    closeCallback: false
  }
};
const initModals = () => {
  const modalElements = document.querySelectorAll('.modal');
  if (modalElements.length) {
    modalElements.forEach(el => {
      setTimeout(() => {
        el.classList.remove('modal--preload');
      }, 100);
    });
  }
  modals = new _modals__WEBPACK_IMPORTED_MODULE_0__["Modals"](settings);
  // Используйте в разработке экспортируемую переменную modals, window сделан для бэкэнда
  window.modals = modals;
};


/***/ }),

/***/ "./js/modules/modals/modals.js":
/*!*************************************!*\
  !*** ./js/modules/modals/modals.js ***!
  \*************************************/
/*! exports provided: Modals */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Modals", function() { return Modals; });
/* harmony import */ var _utils_scroll_lock__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../utils/scroll-lock */ "./js/utils/scroll-lock.js");
/* harmony import */ var _utils_focus_lock__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../utils/focus-lock */ "./js/utils/focus-lock.js");


class Modals {
  constructor(settings = {}) {
    this._scrollLock = new _utils_scroll_lock__WEBPACK_IMPORTED_MODULE_0__["ScrollLock"]();
    this._focusLock = new _utils_focus_lock__WEBPACK_IMPORTED_MODULE_1__["FocusLock"]();
    this._modalOpenElements = document.querySelectorAll('[data-open-modal]');
    this._openedModalElement = null;
    this._modalName = null;
    this._enableScrolling = true;
    this._settingKey = 'default';
    this._settings = settings;
    this._preventDefault = this._settings[this._settingKey].preventDefault;
    this._stopPlay = this._settings[this._settingKey].stopPlay;
    this._lockFocus = this._settings[this._settingKey].lockFocus;
    this._startFocus = this._settings[this._settingKey].startFocus;
    this._focusBack = this._settings[this._settingKey].focusBack;
    this._eventTimeout = this._settings[this._settingKey].eventTimeout;
    this._openCallback = this._settings[this._settingKey].openCallback;
    this._closeCallback = this._settings[this._settingKey].closeCallback;
    this._documentKeydownHandler = this._documentKeydownHandler.bind(this);
    this._documentClickHandler = this._documentClickHandler.bind(this);
    this._modalClickHandler = this._modalClickHandler.bind(this);
    this._init();
  }
  _init() {
    if (this._modalOpenElements.length) {
      document.addEventListener('click', this._documentClickHandler);
    }
  }
  _setSettings(settingKey = this._settingKey) {
    if (!this._settings[settingKey]) {
      return;
    }
    this._preventDefault = typeof this._settings[settingKey].preventDefault === 'boolean' ? this._settings[settingKey].preventDefault : this._settings[this._settingKey].preventDefault;
    this._stopPlay = typeof this._settings[settingKey].stopPlay === 'boolean' ? this._settings[settingKey].stopPlay : this._settings[this._settingKey].stopPlay;
    this._lockFocus = typeof this._settings[settingKey].lockFocus === 'boolean' ? this._settings[settingKey].lockFocus : this._settings[this._settingKey].lockFocus;
    this._startFocus = typeof this._settings[settingKey].startFocus === 'boolean' ? this._settings[settingKey].startFocus : this._settings[this._settingKey].startFocus;
    this._focusBack = typeof this._settings[settingKey].lockFocus === 'boolean' ? this._settings[settingKey].focusBack : this._settings[this._settingKey].focusBack;
    this._eventTimeout = typeof this._settings[settingKey].eventTimeout === 'number' ? this._settings[settingKey].eventTimeout : this._settings[this._settingKey].eventTimeout;
    this._openCallback = this._settings[settingKey].openCallback || this._settings[this._settingKey].openCallback;
    this._closeCallback = this._settings[settingKey].closeCallback || this._settings[this._settingKey].closeCallback;
  }
  _documentClickHandler(evt) {
    const target = evt.target;
    if (!target.closest('[data-open-modal]')) {
      return;
    }
    evt.preventDefault();
    this._modalName = target.closest('[data-open-modal]').dataset.openModal;
    if (!this._modalName) {
      return;
    }
    this.open();
  }
  _documentKeydownHandler(evt) {
    const isEscKey = evt.key === 'Escape' || evt.key === 'Esc';
    if (isEscKey) {
      evt.preventDefault();
      this.close(document.querySelector('.modal.is-active').dataset.modal);
    }
  }
  _modalClickHandler(evt) {
    const target = evt.target;
    if (!target.closest('[data-close-modal]')) {
      return;
    }
    this.close(target.closest('[data-modal]').dataset.modal);
  }
  _addListeners(modal) {
    modal.addEventListener('click', this._modalClickHandler);
    document.addEventListener('keydown', this._documentKeydownHandler);
  }
  _removeListeners(modal) {
    modal.removeEventListener('click', this._modalClickHandler);
    document.removeEventListener('keydown', this._documentKeydownHandler);
  }
  _stopInteractive(modal) {
    if (this._stopPlay) {
      modal.querySelectorAll('video, audio').forEach(el => el.pause());
      modal.querySelectorAll('[data-iframe]').forEach(el => {
        el.querySelector('iframe').contentWindow.postMessage('{"event": "command", "func": "pauseVideo", "args": ""}', '*');
      });
    }
  }
  _autoPlay(modal) {
    modal.querySelectorAll('[data-iframe]').forEach(el => {
      const autoPlay = el.closest('[data-auto-play]');
      if (autoPlay) {
        el.querySelector('iframe').contentWindow.postMessage('{"event":"command","func":"playVideo","args":""}', '*');
      }
    });
  }
  open(modalName = this._modalName) {
    const modal = document.querySelector(`[data-modal="${modalName}"]`);
    if (!modal || modal.classList.contains('is-active')) {
      return;
    }
    document.removeEventListener('click', this._documentClickHandler);
    this._openedModalElement = document.querySelector('.modal.is-active');
    if (this._openedModalElement) {
      this._enableScrolling = false;
      this.close(this._openedModalElement.dataset.modal);
    }
    this._setSettings(modalName);
    modal.classList.add('is-active');
    if (!this._openedModalElement) {
      this._scrollLock.disableScrolling();
    }
    if (this._openCallback) {
      this._openCallback();
    }
    if (this._lockFocus) {
      this._focusLock.lock('.modal.is-active', this._startFocus);
    }
    setTimeout(() => {
      this._addListeners(modal);
      this._autoPlay(modal);
      document.addEventListener('click', this._documentClickHandler);
    }, this._eventTimeout);
  }
  close(modalName = this._modalName) {
    const modal = document.querySelector(`[data-modal="${modalName}"]`);
    document.removeEventListener('click', this._documentClickHandler);
    if (!modal || !modal.classList.contains('is-active')) {
      return;
    }
    if (this._lockFocus) {
      this._focusLock.unlock(this._focusBack);
    }
    modal.classList.remove('is-active');
    this._removeListeners(modal);
    this._stopInteractive(modal);
    if (this._closeCallback) {
      this._closeCallback();
    }
    if (this._enableScrolling) {
      setTimeout(() => {
        this._scrollLock.enableScrolling();
      }, this._eventTimeout);
    }
    setTimeout(() => {
      document.addEventListener('click', this._documentClickHandler);
    }, this._eventTimeout);
    this._setSettings('default');
    this._enableScrolling = true;
  }
}

/***/ }),

/***/ "./js/utils/active-nav.js":
/*!********************************!*\
  !*** ./js/utils/active-nav.js ***!
  \********************************/
/*! exports provided: activeNav */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "activeNav", function() { return activeNav; });
const activeNav = () => {
  const navLinks = document.querySelectorAll('.main-nav__link[href^="#"]');
  const sections = document.querySelectorAll('section[id]');
  if (!navLinks.length || !sections.length) return;
  const updateActiveNav = () => {
    const scrollPosition = window.scrollY + 150; // отступ для header

    let activeSection = null;

    // Проходим по всем секциям и находим активную
    sections.forEach(section => {
      const sectionTop = section.offsetTop;
      const sectionHeight = section.offsetHeight;
      if (scrollPosition >= sectionTop && scrollPosition < sectionTop + sectionHeight) {
        activeSection = section.id;
      }
    });

    // ВСЕГДА убираем активный класс у всех ссылок
    navLinks.forEach(link => {
      link.classList.remove('is-active');
      link.blur(); // снимаем фокус со всех ссылок
    });

    // Если нашли активную секцию, добавляем класс к соответствующей ссылке
    if (activeSection) {
      const activeLink = document.querySelector(`.main-nav__link[href="#${activeSection}"]`);
      if (activeLink) {
        activeLink.classList.add('is-active');
      }
    }
  };

  // Вызываем при скролле
  let scrollTimeout;
  window.addEventListener('scroll', () => {
    clearTimeout(scrollTimeout);
    scrollTimeout = setTimeout(updateActiveNav, 10);
  });

  // Вызываем сразу для определения начального состояния
  updateActiveNav();
};

/***/ }),

/***/ "./js/utils/focus-lock.js":
/*!********************************!*\
  !*** ./js/utils/focus-lock.js ***!
  \********************************/
/*! exports provided: FocusLock */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "FocusLock", function() { return FocusLock; });
const SELECTORS = ['a[href]', 'area[href]', 'input:not([disabled]):not([type="hidden"]):not([aria-hidden])', 'select:not([disabled]):not([aria-hidden])', 'textarea:not([disabled]):not([aria-hidden])', 'button:not([disabled]):not([aria-hidden])', 'iframe', 'object', 'embed', '[contenteditable]', '[tabindex]:not([tabindex^="-"])'];
class FocusLock {
  constructor() {
    this._lockedSelector = null;
    this._focusableElements = null;
    this._endElement = null;
    this._selectors = SELECTORS;
    this._documentKeydownHandler = this._documentKeydownHandler.bind(this);
  }
  _documentKeydownHandler(evt) {
    const activeElement = document.activeElement;
    if (evt.key === 'Tab') {
      if (!this._focusableElements.length) {
        evt.preventDefault();
        activeElement.blur();
        return;
      }
      if (this._focusableElements.length === 1) {
        evt.preventDefault();
        this._focusableElements[0].focus();
        return;
      }
      if (this._focusableElements.length > 1 && !activeElement.closest(this._lockedSelector)) {
        evt.preventDefault();
        this._focusableElements[0].focus();
        return;
      }
    }
    if (evt.key === 'Tab' && !evt.shiftKey && activeElement === this._focusableElements[this._focusableElements.length - 1]) {
      evt.preventDefault();
      this._focusableElements[0].focus();
    }
    if (evt.key === 'Tab' && evt.shiftKey && activeElement === this._focusableElements[0]) {
      evt.preventDefault();
      this._focusableElements[this._focusableElements.length - 1].focus();
    }
  }
  lock(lockedSelector, startFocus = true) {
    this.unlock();
    this._lockedSelector = lockedSelector;
    const lockedElement = document.querySelector(this._lockedSelector);
    if (!lockedElement) {
      return;
    }
    this._focusableElements = lockedElement.querySelectorAll(this._selectors);
    this._endElement = document.activeElement;
    const startElement = lockedElement.querySelector('[data-focus]') || this._focusableElements[0];
    if (this._endElement) {
      this._endElement.blur();
    }
    if (startElement && startFocus) {
      startElement.focus();
    }
    document.addEventListener('keydown', this._documentKeydownHandler);
  }
  unlock(returnFocus = true) {
    if (this._endElement && returnFocus) {
      this._endElement.focus();
    }
    this._lockedSelector = null;
    this._focusableElements = null;
    this._endElement = null;
    document.removeEventListener('keydown', this._documentKeydownHandler);
  }
}
window.focusLock = new FocusLock();

/***/ }),

/***/ "./js/utils/ios-checker.js":
/*!*********************************!*\
  !*** ./js/utils/ios-checker.js ***!
  \*********************************/
/*! exports provided: iosChecker */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "iosChecker", function() { return iosChecker; });
const iosChecker = () => {
  return ['iPad Simulator', 'iPhone Simulator', 'iPod Simulator', 'iPad', 'iPhone', 'iPod'].includes(navigator.platform)
  // iPad on iOS 13 detection
  || navigator.userAgent.includes('Mac') && 'ontouchend' in document;
};

/***/ }),

/***/ "./js/utils/ios-vh-fix.js":
/*!********************************!*\
  !*** ./js/utils/ios-vh-fix.js ***!
  \********************************/
/*! exports provided: iosVhFix */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "iosVhFix", function() { return iosVhFix; });
/* harmony import */ var _ios_checker__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ios-checker */ "./js/utils/ios-checker.js");

const iosVhFix = () => {
  if (!(!!window.MSInputMethodContext && !!document.documentMode)) {
    if (Object(_ios_checker__WEBPACK_IMPORTED_MODULE_0__["iosChecker"])()) {
      let vh = window.innerHeight * 0.01;
      document.documentElement.style.setProperty('--vh', `${vh}px`);
      window.addEventListener('resize', function () {
        vh = window.innerHeight * 0.01;
        document.documentElement.style.setProperty('--vh', `${vh}px`);
      });
    }
  }
};


/***/ }),

/***/ "./js/utils/scroll-lock.js":
/*!*********************************!*\
  !*** ./js/utils/scroll-lock.js ***!
  \*********************************/
/*! exports provided: ScrollLock */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "ScrollLock", function() { return ScrollLock; });
/* harmony import */ var _ios_checker__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ios-checker */ "./js/utils/ios-checker.js");

class ScrollLock {
  constructor() {
    this._iosChecker = _ios_checker__WEBPACK_IMPORTED_MODULE_0__["iosChecker"];
    this._lockClass = this._iosChecker() ? 'scroll-lock-ios' : 'scroll-lock';
    this._scrollTop = null;
    this._fixedBlockElements = document.querySelectorAll('[data-fix-block]');
  }
  _getScrollbarWidth() {
    return window.innerWidth - document.documentElement.clientWidth;
  }
  _getBodyScrollTop() {
    return self.pageYOffset || document.documentElement && document.documentElement.ScrollTop || document.body && document.body.scrollTop;
  }
  disableScrolling() {
    this._scrollTop = document.body.dataset.scroll = document.body.dataset.scroll ? document.body.dataset.scroll : this._getBodyScrollTop();
    if (this._getScrollbarWidth()) {
      document.body.style.paddingRight = `${this._getScrollbarWidth()}px`;
      this._fixedBlockElements.forEach(block => {
        block.style.paddingRight = `${this._getScrollbarWidth()}px`;
      });
    }
    document.body.style.top = `-${this._scrollTop}px`;
    document.body.classList.add(this._lockClass);
  }
  enableScrolling() {
    document.body.classList.remove(this._lockClass);
    window.scrollTo(0, +document.body.dataset.scroll);
    document.body.style.paddingRight = null;
    document.body.style.top = null;
    this._fixedBlockElements.forEach(block => {
      block.style.paddingRight = null;
    });
    document.body.removeAttribute('data-scroll');
    this._scrollTop = null;
  }
}
window.scrollLock = new ScrollLock();

/***/ }),

/***/ "./js/utils/smoothScroll.js":
/*!**********************************!*\
  !*** ./js/utils/smoothScroll.js ***!
  \**********************************/
/*! exports provided: smoothScroll */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "smoothScroll", function() { return smoothScroll; });
const smoothScroll = () => {
  const links = document.querySelectorAll('.main-nav__link[href^="#"]');
  links.forEach(link => {
    link.addEventListener('click', e => {
      e.preventDefault();
      const targetId = link.getAttribute('href');
      const targetElement = document.querySelector(targetId);
      if (targetElement) {
        const headerHeight = 92; // высота header
        const targetPosition = targetElement.offsetTop - headerHeight;
        window.scrollTo({
          top: targetPosition,
          behavior: 'smooth'
        });
      }
    });
  });
};


/***/ }),

/***/ "./js/utils/telegram.js":
/*!******************************!*\
  !*** ./js/utils/telegram.js ***!
  \******************************/
/*! exports provided: openTelegram, bindTelegramLinks */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "openTelegram", function() { return openTelegram; });
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "bindTelegramLinks", function() { return bindTelegramLinks; });
const openTelegram = (username, text) => {
  if (!username) {
    return;
  }
  const encodedText = encodeURIComponent(text || "");
  const deepLink = `tg://resolve?domain=${username}&text=${encodedText}`;
  const webLink = `https://t.me/${username}?text=${encodedText}`;
  let fallbackTimer = null;
  const clear = () => {
    if (fallbackTimer) {
      clearTimeout(fallbackTimer);
      fallbackTimer = null;
    }
    document.removeEventListener("visibilitychange", onVisibilityChange);
    window.removeEventListener("pagehide", onPageHide);
    window.removeEventListener("blur", onBlur);
  };
  const onVisibilityChange = () => {
    if (document.visibilityState === "hidden") {
      clear();
    }
  };
  const onPageHide = () => clear();
  const onBlur = () => clear();

  // Schedule fallback to web link in the same tab (less likely to be blocked)
  fallbackTimer = setTimeout(() => {
    window.location.href = webLink;
    clear();
  }, 900);
  document.addEventListener("visibilitychange", onVisibilityChange, {
    once: false
  });
  window.addEventListener("pagehide", onPageHide, {
    once: true
  });
  window.addEventListener("blur", onBlur, {
    once: true
  });

  // Try deep link (same tab)
  try {
    window.location.href = deepLink;
  } catch (e) {
    // If blocked, fallback will trigger
  }
};
const bindTelegramLinks = username => {
  if (!username) {
    return;
  }
  document.addEventListener("click", event => {
    const target = event.target instanceof Element ? event.target : null;
    if (!target) {
      return;
    }
    const link = target.closest("a[href]");
    if (!link) {
      return;
    }
    const href = link.getAttribute("href") || "";
    // Match links pointing to this username on t.me
    if (/^https:\/\/t\.me\//.test(href) && href.includes(`/${username}`)) {
      event.preventDefault();
      let text = "";
      try {
        const url = new URL(href, window.location.origin);
        text = url.searchParams.get("text") || link.getAttribute("data-telegram-text") || "";
      } catch (_) {
        text = link.getAttribute("data-telegram-text") || "";
      }
      openTelegram(username, text);
    }
  }, {
    capture: true
  });
};

/***/ })

/******/ });
//# sourceMappingURL=main.min.js.map